# Hand software (Python 3.12). Setup:
#   python -m pip install uv && python -m uv venv --python 3.12 .venv
#   python -m uv pip install --python .venv/Scripts/python.exe -r requirements.txt
#   python -m uv pip install --python .venv/Scripts/python.exe torch --index-url https://download.pytorch.org/whl/cu126
mujoco>=3.3
numpy
pyserial
opencv-contrib-python
pytest

# optional: NVIDIA MuJoCo Warp, GPU batch simulation (works on any CUDA GPU incl. GTX 1660 Ti)
mujoco-warp

# optional: voice (tools/listen.py)
sounddevice
soundfile
# optional: local VLM server (tools/local_vlm_server.py)
transformers
accelerate
bitsandbytes
# optional: real InMoov meshes in the full-body sim (tools/fetch_inmoov_meshes.py)
trimesh
pycollada
